#amara jodi page ar all images ber korte chai👇
console.log(document.images)


#Jodi all image url lage niser code copy koro
[...document.querySelectorAll('img')].map(img => img.src || img.getAttribute('data-src'));
